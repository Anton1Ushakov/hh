services:
  hh-analyzer:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: "8000"
      DATABASE_URL: sqlite:////data/hh_analyzer.db
    volumes:
      - hh_data:/data

volumes:
  hh_data:
